# Interface contract

The console provides Decision Desk, Inference Lab, Variant Registry, Benchmark Matrix and
Evidence Room. All figures come from API-served reports. Status is visible through text and
shape, not color alone. Tables remain scrollable on small screens, navigation is keyboard
accessible, controls have labels and reduced-motion preferences disable decorative movement.
