# AWS App Runner release

Project 24 targets immutable ECR images under `plan-04/p24-edge-vision-console` and the
semantic App Runner service `ai-04-p24-edge-vision-console`. The script performs only local
evidence validation unless `-ValidateAws` or `-Apply` is explicitly supplied.
