# Interface contract

The studio exposes five connected views: retrieval, refinement, benchmark, failure atlas and
system cards. All metrics come from API reports. Results always show image IDs, evidence
captions and score components. Keyboard focus remains visible, images have alternative text,
the layout supports 390/768/1440-pixel viewports and reduced motion disables animation.

